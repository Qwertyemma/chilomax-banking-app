<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fund Account - Chilomax Bank</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            color: #333;
        }

        .container {
            max-width: 800px;
            margin: 20px auto;
            padding: 20px;
            background: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        h1, h2, h3 {
            color: #2c3e50;
        }

        .balance {
            font-size: 1.2em;
            font-weight: bold;
            color: #27ae60;
        }

        .account-info {
            margin-top: 20px;
            padding: 15px;
            border: 1px solid #ddd;
            border-radius: 5px;
            background-color: #fafafa;
        }

        .account-info p {
            margin: 10px 0;
        }

        .error-message {
            color: #e74c3c;
            font-size: 1em;
        }

        .note {
            margin-top: 20px;
            font-size: 1.1em;
            color: #3498db;
        }
    </style>
</head>

<body>
    <div class="container">
        <h1>Fund Your Wallet</h1>

        <p>Your current balance: <span class="balance"><%= balance %></span></p>

        <% if (!userAccountNumber) { %>
            <p>Generating your virtual account. Please wait...</p>
        <% } else { %>
            <div class="account-info">
                <h2>Transfer to the following bank account:</h2>
                <p><strong>Bank:</strong> <%= userAccountBank %></p>
                <p><strong>Account Number:</strong> <%= userAccountNumber %></p>
            </div>

            <p class="note">Once your transfer is complete, your wallet will be automatically updated.</p>

            <% if (error) { %>
                <p class="error-message"><%= error %></p>
            <% } %>
        <% } %>
    </div>
</body>

</html>