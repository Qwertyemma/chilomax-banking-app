<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Buy Data</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        .form-container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 8px;
            background-color: #f9f9f9;
        }
        .form-header {
            margin-bottom: 20px;
            border-bottom: 1px solid #ddd;
            padding-bottom: 10px;
        }
        .btn-custom {
            background-color: #007bff;
            color: #fff;
        }
        .btn-custom:hover {
            background-color: #0056b3;
            color: #fff;
        }
    </style>
</head>
<body>
    <div class="container mt-4">
        <div class="form-container">
            <div class="form-header">
                <h2>Buy Data</h2>
                <p class="lead">Balance: ₦<%= balance %></p>
            </div>
            <form action="/dashboard/buy-data" method="POST">
                <div class="form-group">
                    <label for="provider">Select Service Provider:</label>
                    <select class="form-control" id="provider" name="provider" required>
                        <option value="" disabled selected>Select a provider</option>
                        <option value="mtn">MTN</option>
                        <option value="glo">Glo</option>
                        <option value="airtel">Airtel</option>
                        <option value="9mobile">9Mobile</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="plan">Select Data Package:</label>
                    <select class="form-control" id="plan" name="plan" required>
                        <option value="" disabled selected>Select a package</option>
                        <option value="100">₦100 - 1GB</option>
                        <option value="200">₦200 - 2GB</option>
                        <option value="500">₦500 - 5GB</option>
                        <option value="1000">₦1000 - 10GB</option>
                    </select>
                </div>
                <div class="form-group">
                    <label for="phone">Enter Phone Number:</label>
                    <input type="text" class="form-control" id="phone" name="phone" required>
                </div>
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-danger">
                        <%= error %>
                    </div>
                <% } %>
                <button type="submit" class="btn btn-custom">Buy Data</button>
            </form>
        </div>
    </div>
</body>
</html>